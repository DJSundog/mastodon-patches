This patch adds CSS styles to collapse the height of individual notifications for long posts.

Patch truncates any notification taller than 100px. Notification is expanded on hover, re-truncated on blur.
